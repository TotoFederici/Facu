#include <string.h>
#include <stdlib.h>
#include <stdio.h>

int main() {
    char* a = malloc(sizeof(char)); //malloc es del tipo void*, y si no puede reservar espacio, devuelve NULL :)
    return 0;
}